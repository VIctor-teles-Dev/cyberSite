# Copy this file to `.env` (or create `.env.local`) and fill with your key.
# Do NOT commit your real key. Keep `.env` out of version control.

# For client-side Vite usage (NOT recommended for public keys):
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# For server-side usage (recommended):
API_KEY=your_gemini_api_key_here
